export type FooterProps = {
    title?: string,
    description?: string,
    links?: string,
    company?: string[],
    companyTitle?: string,
    region?: string[],
    regionTitle?: string,
    help?: string[],
    helpTitle?: string
}